import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    gap: 8,
    alignItems: "center",
  },
  textNameParticipant: {
    flex: 1,
    backgroundColor: "#27272A",
    color: "#F4F4F5",
    fontSize: 18,
    height: 54,
    borderRadius: 4,
    padding: 12,
  },
});
